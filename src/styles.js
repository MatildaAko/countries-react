import { useTheme} from "./ThemeContext";
const Styles = () => {
}
const themeStylesMain = {
  const darkTheme = useTheme();
  backgroundColor: darkTheme ? "blue" : "#CCC",
  color: darkTheme ? "#CCC" : "#333",
};


export {Styles, themeStylesMain}